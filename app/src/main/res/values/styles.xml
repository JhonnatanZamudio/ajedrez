<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CustomDialogTheme" parent="Theme.AppCompat.Dialog.Alert">
        <!--buttons color-->
        <item name="colorAccent">#ffffff</item>
        <!--title and message color-->
        <item name="android:textColorPrimary">#000000</item>
        <!--dialog background-->
        <item name="android:windowBackground">@drawable/background_dialog</item>
        <item name="android:windowTitleStyle">@style/PauseDialogTitle</item>
        <item name="fontFamily">@font/helveticaltstdroman</item>
        <item name="android:paddingBottom">2dp</item>
    </style>

    <style name="PauseDialogTitle" parent="TextAppearance.AppCompat.Headline">
        <item name="android:textAlignment" tools:targetApi="jelly_bean_mr1">center</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textColor">#428980</item>
        <item name="fontFamily">@font/helveticaltstdbold</item>
    </style>

</resources>